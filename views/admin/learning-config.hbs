<!-- Configurações Learning Module — 3 Interactive Lessons -->
<div class="learning-module" data-module="config" data-total="3">

 <!-- Header -->
 <div class="mb-4 p-5 flex flex-col lg:flex-row lg:items-center justify-between gap-4" style="background:#151515; border-radius:5px; box-shadow:0 2px 8px rgba(0,0,0,0.6); border:1px solid rgba(255,255,255,0.06);">
  <div class="flex items-center gap-3">
   <a href="/admin/aprendizado" class="w-8 h-8 rounded-lg bg-white/5 border border-white/10 flex items-center justify-center text-gray-400 hover:text-white transition-all">
    <span class="material-symbols-outlined text-sm">arrow_back</span>
   </a>
   <div>
    <h1 class="text-xl font-black text-white tracking-tight">Configurações</h1>
    <p class="text-[10px] font-bold text-gray-500 uppercase tracking-widest mt-0.5">Conta, Usuários e Integrações</p>
   </div>
  </div>
  <div class="flex items-center gap-4">
   <div class="text-right">
    <p class="text-[9px] font-bold text-gray-500 uppercase mb-1">Progresso</p>
    <div class="w-32 h-2 rounded-full bg-white/5 overflow-hidden">
     <div id="progressBar" class="h-full bg-gradient-to-r from-gray-500 to-gray-400 transition-all" style="width:0%"></div>
    </div>
   </div>
   <span class="text-sm font-black text-gray-400" id="stepCounter">0/3</span>
  </div>
 </div>

 <!-- Lesson Steps Container -->
 <div id="lessonsContainer" class="space-y-3">

  <!-- LESSON 1: Perfil e Personalização -->
  <section class="lesson-step" data-step="1">
   <div class="kcard p-0 overflow-hidden">
    <div class="grid grid-cols-1 lg:grid-cols-5 gap-0">
     <div class="lg:col-span-3 p-6 space-y-4">
      <h2 class="text-xl font-black text-white">Perfil e Personalização</h2>
      <p class="text-sm text-gray-300 leading-relaxed">
       Customize sua experiência no Malha3D. Acesse Configurações → Perfil para: alterar avatar, nome completo, tema (Modo Claro/Escuro) e cor de destaque. Suas preferências são salvas automaticamente e sincronizadas em todos os dispositivos.
      </p>
      <ul class="text-sm text-gray-300 space-y-1.5">
       <li>🎨 <strong>Avatar:</strong> Foto de perfil com upload</li>
       <li>📝 <strong>Nome:</strong> Como você aparece na equipe</li>
       <li>🌓 <strong>Tema:</strong> Modo Claro ou Escuro (default: Escuro)</li>
       <li>🟠 <strong>Cor Destaque:</strong> Laranja, Azul, Verde, etc.</li>
      </ul>
      <div class="p-3 rounded bg-gray-500/10 border border-gray-500/20">
       <p class="text-xs text-gray-200"><strong>💡 Dica:</strong> Use o Modo Claro se trabalha em ambientes muito claros — reduz fadiga ocular.</p>
      </div>
     </div>
     <div class="lg:col-span-2 bg-black/30 p-5 flex flex-col items-center justify-center border-l border-white/5 min-h-[300px]">
      <div class="w-full text-center space-y-3">
       <div class="text-[10px] font-black text-gray-500 uppercase mb-2">Painel de Personalização</div>
       <div class="space-y-2">
        <div class="flex items-center gap-2">
         <div class="w-10 h-10 rounded-lg bg-orange-500/30 border border-orange-500/50 flex items-center justify-center text-xl font-black text-white">M</div>
         <div class="text-left flex-1">
          <p class="text-xs font-bold text-gray-300">Seu Nome</p>
          <p class="text-[10px] text-gray-500">Admin</p>
         </div>
        </div>
        <div class="px-3 py-2 rounded bg-white/5 border border-white/10">
         <p class="text-[9px] font-bold text-gray-300 mb-1">Tema</p>
         <div class="flex gap-1">
          <button class="px-2 py-1 rounded text-[9px] font-bold bg-white/10 text-gray-300">Escuro</button>
          <button class="px-2 py-1 rounded text-[9px] font-bold bg-white/20 text-gray-300">Claro</button>
         </div>
        </div>
        <div class="px-3 py-2 rounded bg-white/5 border border-white/10">
         <p class="text-[9px] font-bold text-gray-300 mb-1">Cor Destaque</p>
         <div class="flex gap-1">
          <div class="w-5 h-5 rounded bg-orange-500 cursor-pointer border-2 border-white/50"></div>
          <div class="w-5 h-5 rounded bg-blue-500 cursor-pointer border border-white/20"></div>
          <div class="w-5 h-5 rounded bg-green-500 cursor-pointer border border-white/20"></div>
          <div class="w-5 h-5 rounded bg-purple-500 cursor-pointer border border-white/20"></div>
         </div>
        </div>
       </div>
      </div>
     </div>
    </div>
    <div class="px-5 py-3 border-t border-white/5 flex items-center justify-between">
     <label class="flex items-center gap-2 cursor-pointer">
      <input type="checkbox" onchange="markComplete('config', 1)" class="w-4 h-4 rounded border-white/20 accent-gray-400">
      <span class="text-xs font-bold text-gray-400">Entendi esta etapa</span>
     </label>
    </div>
   </div>
  </section>

  <!-- LESSON 2: Usuários e Permissões -->
  <section class="lesson-step hidden" data-step="2">
   <div class="kcard p-0 overflow-hidden">
    <div class="grid grid-cols-1 lg:grid-cols-5 gap-0">
     <div class="lg:col-span-3 p-6 space-y-4">
      <h2 class="text-xl font-black text-white">Usuários e Permissões</h2>
      <p class="text-sm text-gray-300 leading-relaxed">
       Gerencie sua equipe aqui. Você pode adicionar novos usuários com diferentes roles: Admin (controle total), Staff (acesso de produtor) ou Collaborator (acesso limitado). Cada role tem permissões específicas por módulo.
      </p>
      <ul class="text-sm text-gray-300 space-y-1.5">
       <li>👨‍💼 <strong>Admin:</strong> Configurações, usuários, backups</li>
       <li>👷 <strong>Staff:</strong> Acesso a CRM, Projetos e Financeiro</li>
       <li>🤝 <strong>Collaborator:</strong> Acesso limitado a tarefas</li>
       <li>⚙️ <strong>Permissões:</strong> Customize por módulo por role</li>
      </ul>
      <div class="p-3 rounded bg-gray-500/10 border border-gray-500/20">
       <p class="text-xs text-gray-200"><strong>💡 Dica:</strong> Use Collaborator para consultores — eles veem apenas o que precisam.</p>
      </div>
     </div>
     <div class="lg:col-span-2 bg-black/30 p-5 flex flex-col items-center justify-center border-l border-white/5 min-h-[300px]">
      <div class="w-full text-center space-y-2">
       <div class="text-[10px] font-black text-gray-500 uppercase mb-2">Gerenciar Usuários</div>
       <div class="space-y-1.5 text-left">
        <div class="px-3 py-2 rounded bg-white/5 border border-white/10 flex items-center justify-between">
         <div>
          <p class="text-xs font-bold text-gray-300">João Silva</p>
          <p class="text-[9px] text-gray-500">Admin</p>
         </div>
         <span class="material-symbols-outlined text-sm text-gray-500">delete</span>
        </div>
        <div class="px-3 py-2 rounded bg-white/5 border border-white/10 flex items-center justify-between">
         <div>
          <p class="text-xs font-bold text-gray-300">Maria Santos</p>
          <p class="text-[9px] text-gray-500">Staff</p>
         </div>
         <span class="material-symbols-outlined text-sm text-gray-500">delete</span>
        </div>
        <button class="w-full mt-2 px-3 py-2 rounded bg-gray-500/20 border border-gray-500/30 text-xs font-bold text-gray-300 hover:bg-gray-500/30 transition-all">
         + Adicionar Usuário
        </button>
       </div>
      </div>
     </div>
    </div>
    <div class="px-5 py-3 border-t border-white/5 flex items-center justify-between">
     <label class="flex items-center gap-2 cursor-pointer">
      <input type="checkbox" onchange="markComplete('config', 2)" class="w-4 h-4 rounded border-white/20 accent-gray-400">
      <span class="text-xs font-bold text-gray-400">Entendi esta etapa</span>
     </label>
    </div>
   </div>
  </section>

  <!-- LESSON 3: Integrações e API -->
  <section class="lesson-step hidden" data-step="3">
   <div class="kcard p-0 overflow-hidden">
    <div class="grid grid-cols-1 lg:grid-cols-5 gap-0">
     <div class="lg:col-span-3 p-6 space-y-4">
      <h2 class="text-xl font-black text-white">Integrações e API</h2>
      <p class="text-sm text-gray-300 leading-relaxed">
       Conecte ferramentas externas ao Malha3D: WhatsApp (envio automático), Google Calendar (sincronização), Email SMTP (notificações), etc. Também disponibilizamos API própria com chaves de acesso para desenvolvedores.
      </p>
      <ul class="text-sm text-gray-300 space-y-1.5">
       <li>💬 <strong>WhatsApp:</strong> Mensagens automatizadas para leads</li>
       <li>📅 <strong>Google Calendar:</strong> Sincronize compromissos</li>
       <li>📧 <strong>SMTP Email:</strong> Configure servidor de emails próprio</li>
       <li>🔑 <strong>API Keys:</strong> Para integrações customizadas</li>
      </ul>
      <div class="p-3 rounded bg-gray-500/10 border border-gray-500/20">
       <p class="text-xs text-gray-200"><strong>💡 Dica:</strong> Gere API keys com permissões limitadas — nunca compartilhe a chave master.</p>
      </div>
     </div>
     <div class="lg:col-span-2 bg-black/30 p-5 flex flex-col items-center justify-center border-l border-white/5 min-h-[300px]">
      <div class="w-full text-center space-y-2">
       <div class="text-[10px] font-black text-gray-500 uppercase mb-2">Integrações</div>
       <div class="space-y-1.5">
        <div class="px-3 py-2 rounded bg-white/5 border border-white/10 flex items-center gap-2">
         <span class="material-symbols-outlined text-sm text-green-400">check_circle</span>
         <span class="text-xs font-bold text-gray-300 flex-1 text-left">WhatsApp</span>
         <span class="text-[9px] text-gray-500">Ativo</span>
        </div>
        <div class="px-3 py-2 rounded bg-white/5 border border-white/10 flex items-center gap-2">
         <span class="material-symbols-outlined text-sm text-gray-500">toggle_off</span>
         <span class="text-xs font-bold text-gray-300 flex-1 text-left">Google Calendar</span>
         <span class="text-[9px] text-gray-500">Inativo</span>
        </div>
        <div class="px-3 py-2 rounded bg-white/5 border border-white/10 flex items-center gap-2">
         <span class="material-symbols-outlined text-sm text-gray-500">toggle_off</span>
         <span class="text-xs font-bold text-gray-300 flex-1 text-left">Email SMTP</span>
         <span class="text-[9px] text-gray-500">Inativo</span>
        </div>
       </div>
      </div>
     </div>
    </div>
    <div class="px-5 py-3 border-t border-white/5 flex items-center justify-between">
     <label class="flex items-center gap-2 cursor-pointer">
      <input type="checkbox" onchange="markComplete('config', 3)" class="w-4 h-4 rounded border-white/20 accent-gray-400">
      <span class="text-xs font-bold text-gray-400">Entendi esta etapa</span>
     </label>
    </div>
   </div>
  </section>

 </div>

 <!-- Navigation Buttons -->
 <div class="flex items-center justify-between gap-3 mt-6">
  <button id="btnPrev" onclick="previousStep('config')" class="px-4 py-2 rounded-lg bg-white/5 border border-white/10 text-white text-xs font-bold uppercase hover:bg-white/10 transition-all">
   <span class="material-symbols-outlined text-sm align-middle mr-1">arrow_back</span> Anterior
  </button>
  <div class="flex items-center gap-2">
   <button onclick="goToStep('config', 1)" class="w-3 h-3 rounded-full bg-gray-400 transition-all" data-step="1"></button>
   <button onclick="goToStep('config', 2)" class="w-3 h-3 rounded-full bg-white/20 transition-all" data-step="2"></button>
   <button onclick="goToStep('config', 3)" class="w-3 h-3 rounded-full bg-white/20 transition-all" data-step="3"></button>
  </div>
  <button id="btnNext" onclick="nextStep('config')" class="px-4 py-2 rounded-lg bg-gray-500 text-white text-xs font-bold uppercase hover:bg-gray-600 transition-all">
   Próximo <span class="material-symbols-outlined text-sm align-middle ml-1">arrow_forward</span>
  </button>
 </div>

</div>

<script>
(function() {
  const STORAGE_KEY = 'malha3d_learning_progress';
  const MODULE = 'config';
  const TOTAL_STEPS = 3;

  function getProgress() {
    try { return JSON.parse(localStorage.getItem(STORAGE_KEY)) || {}; } catch(e) { return {}; }
  }

  function saveProgress(module, completedSteps) {
    try {
      const progress = getProgress();
      progress[module] = completedSteps;
      localStorage.setItem(STORAGE_KEY, JSON.stringify(progress));
    } catch(e) { console.error('Save progress error:', e); }
  }

  function updateUI() {
    const progress = getProgress();
    const completed = (progress[MODULE] || []).length;
    const percentage = Math.round((completed / TOTAL_STEPS) * 100);

    const progressBar = document.getElementById('progressBar');
    const stepCounter = document.getElementById('stepCounter');
    if (progressBar) progressBar.style.width = percentage + '%';
    if (stepCounter) stepCounter.textContent = completed + '/' + TOTAL_STEPS;
  }

  window.markComplete = function(module, step) {
    const progress = getProgress();
    if (!progress[module]) progress[module] = [];
    if (!progress[module].includes(step)) {
      progress[module].push(step);
      saveProgress(module, progress[module]);
      updateUI();
    }
  };

  window.nextStep = function(module) {
    const current = parseInt(document.querySelector('.lesson-step:not(.hidden)')?.getAttribute('data-step') || 0);
    if (current < TOTAL_STEPS) goToStep(module, current + 1);
  };

  window.previousStep = function(module) {
    const current = parseInt(document.querySelector('.lesson-step:not(.hidden)')?.getAttribute('data-step') || 0);
    if (current > 1) goToStep(module, current - 1);
  };

  window.goToStep = function(module, step) {
    document.querySelectorAll('.lesson-step').forEach(s => s.classList.add('hidden'));
    document.querySelector(`.lesson-step[data-step="${step}"]`)?.classList.remove('hidden');
    document.querySelectorAll('[data-step]').forEach(btn => {
      btn.classList.toggle('bg-gray-400', btn.getAttribute('data-step') == step);
      btn.classList.toggle('bg-white/20', btn.getAttribute('data-step') != step);
    });
  };

  updateUI();
})();
</script>
